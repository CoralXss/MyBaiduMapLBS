1、需要在LBS云后台自定义数据库，如创建geo_db(119162),119162就是"geotable_id"的值，自己要在demo中替换。

2、自定义项目所需的字段，并且可以自己导入或者创建实际数据。

3、在代码中通过lbs云提供的检索接口，自定义搜索条件，可以从你在百度云后台创建的数据库中获取数据。

4、云存储，就是将自己定义的数据存储在百度云上，然后根据百度云提供的接口在自己定义的数据库中检索数据。